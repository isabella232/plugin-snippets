[
    {
        "markdown": "Configuration details for the Release Tracker Plugin."
    },
    {
        "key": "posthogHost",
        "hint": "Optional - defaults to https://app.posthog.com",
        "name": "URL of your PostHog instance",
        "type": "string",
        "order": 1,
        "default": "https://app.posthog.com",
        "required": false
    },
    {
        "key": "posthogApiKey",
        "hint": "Your Personal API Key is not the same as the Project API key",
        "name": "PostHog API Key",
        "type": "string",
        "order": 2,
        "default": "",
        "required": true
    },
    {
        "key": "gitlabHost",
        "hint": "Optional - defaults to https://gitlab.com",
        "name": "URL of your GitLab instance",
        "type": "string",
        "order": 3,
        "default": "https://gitlab.com",
        "required": false
    },
    {
        "key": "gitlabProjectId",
        "hint": "",
        "name": "Unique ID of your GitLab Project",
        "type": "string",
        "order": 4,
        "default": "",
        "required": true
    },
    {
        "key": "gitlabToken",
        "hint": "Required for private repos only",
        "name": "GitLab Personal Token",
        "type": "string",
        "order": 5,
        "default": "",
        "required": false
    }
]
